# Rekindle + Slint

A Phoenix application using Rekindle to run the same Slint example in the
browser and as a native desktop application.

```sh
mix setup
mix phx.server
```

Open <http://localhost:4000>.

Run the desktop target with:

```sh
mix rekindle.dev desktop
```
